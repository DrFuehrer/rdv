When monitoring and then you disconnect, the player only transitions
to the stopped state and not disconnected.

Snapshot doesn't work for time series data panel.

When the jfreechart data panels are unpinned, they
don't always show the icons with a mouseover.

When the channel list is manually resized to 0, it
is in effect hidden, and the view menu item should
indicate this.

Location bar is still jumpy

The data panels still refresh data when they don't need too.